# FabZClean Production Environment Configuration
# Copy this file to .env and fill in your actual values

# Environment
NODE_ENV=production

# Server Configuration
# Host to bind to (0.0.0.0 allows connections from any IP address)
HOST=0.0.0.0

# Port to run the server on (default: 5000)
PORT=5000

# Your static IP address (for access and CORS configuration)
# Configured IP: 117.218.59.207
STATIC_IP=117.218.59.207

# CORS Configuration
# Comma-separated list of allowed origins (optional, static IP is auto-added if STATIC_IP is set)
# The static IP will be automatically added to allowed origins
ALLOWED_ORIGINS=http://117.218.59.207,http://117.218.59.207:5000

# Database Configuration
# For SQLite, DATABASE_URL should point to the database file
# Default location: ./fabzclean.db (relative to project root)
DATABASE_URL=./fabzclean.db

# Or specify absolute path:
# DATABASE_URL=/opt/fabzclean/data/fabzclean.db

# External API Configuration (Optional)
# EXTERNAL_API_KEY=your-api-key-here
# EXTERNAL_API_BASE_URL=https://api.example.com
